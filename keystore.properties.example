# Copy to keystore.properties (gitignored) for local release builds.
# Prefer loading secrets from ~/.config/textimagecleaner-signing/credentials.env instead.
#
# storeFile=/absolute/path/to/textimagecleaner-release.jks
# storePassword=...
# keyAlias=textimagecleaner
# keyPassword=...
